% Run this command to add the present working directory and its
% subdirectories to the Matlab search path before opening any of the
% (AppDesigner) .mlapp files.
% If these directories have not been added to the path, AppDesigner cannot
% find components that are used by the opened .mlapp file.
addpath(genpath(pwd));
